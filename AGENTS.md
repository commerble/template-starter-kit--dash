# AGENTS.md

このファイルは、AIエージェント（あなた）がこのプロジェクトの構造、目的、および開発フローを理解するためのガイドです。

## プロジェクト概要

**Template Starter Kit for Dash** は、**Commerble CMS** 用のテンプレート開発スターターキットです。
主な目的は、ローカル環境でのSCSS/JS開発と、Commerbleプラットフォームへのテンプレート同期を自動化することです。

## ディレクトリ構造と役割

*   **`scss/`**: スタイルシートのソースコード。
    *   `style.scss`: メインのエントリーポイント。
    *   `components/`: 各UIコンポーネントのスタイル。
    *   `controls/`: フォームコントロールなどのスタイル。
*   **`src/`**: JavaScriptのソースコード。
    *   `main.js`: メインのエントリーポイント。
*   **`templates/`**: Commerble CMS用のテンプレートファイル（Razor構文 `.cshtml`）。
    *   **注意**: ビルドプロセスにより、`scss` と `src` の成果物（CSS/JS）もここに配置される場合があります（例: `templates/Bundle/`）。
    *   `Mail/`: メールテンプレート。
    *   `Modd/`: モジュール関連のテンプレート。
    *   `Layout/`: 共通レイアウト。
*   **`build/`**: Webpack用のカスタムローダーなど。
*   **`sync.js`**: ローカルのテンプレートファイルをCommerble CMSのAPI経由で同期するためのNode.jsスクリプト。
*   **`webpack.config.js`**: アセットのバンドル設定。
*   **`.knowledge/`**: AIエージェントが参照すべきドメイン知識や仕様書が格納されています。

## 開発ワークフロー

AIエージェントとしてタスクを実行する際は、以下のスクリプトとフローを意識してください。

1.  **開発モード (`npm start`)**:
    *   ファイルの変更を監視 (`watch`) し、自動的にビルドと同期を行います。
    *   通常、開発中の修正はこのモードで行われていると想定してください。
2.  **ビルド (`npm run build`)**:
    *   本番用にアセットを最適化（ミニファイなど）してビルドします。
3.  **同期 (`npm run upload`)**:
    *   すべてのテンプレートを強制的にアップロードします。

## 重要な設定ファイル

*   **`.env`**: APIエンドポイントと認証情報が含まれます（セキュリティ上の理由から読み取りには注意が必要ですが、存在は認識してください）。
*   **`sync.js`**: 同期ロジックの中核。
    *   `mailTemplatePrefix`: メールテンプレートの識別子（デフォルト: `'Mail'`）。
    *   `mailSharedTemplatePath`: メールテンプレートに結合される共有ファイル。
    *   `ignoreTemplates`: 同期から除外されるファイル。

## コーディング規約と注意点

### 1. テンプレート (Razor / .cshtml)
*   Commerble CMSはASP.NET MVCベースのRazor構文を使用します。
*   `@model` ディレクティブやC#のコードブロックが含まれます。
*   **片方向同期**: ローカルからサーバーへの一方通行です。サーバー上の変更はローカルに反映されないため、常にローカルが正となるように扱ってください。

### 2. スタイル (SCSS)
*   コンポーネント指向で分割されています。新しいUI要素を追加する場合は、`scss/components/` に新しいファイルを作成し、`style.scss` でインポートすることを検討してください。

### 3. JavaScript
*   Webpackでバンドルされます。
*   jQueryが依存関係に含まれており、レガシーなコードやプラグインとの互換性が考慮されている可能性があります。

### 4. メールテンプレート
*   `templates/Mail/` 以下のファイルは特殊な扱いを受けます。
*   同期時に `SharedFunctions.cshtml` の内容が結合される処理が `sync.js` に記述されています。メールテンプレートを編集する際は、この結合ロジックを破壊しないように注意してください。

## AIエージェントへの指示

*   **ファイルの編集**: ユーザーから修正依頼があった場合、`templates/` 内の `.cshtml` ファイル、または `scss/`, `src/` 内のソースファイルを編集してください。ビルド成果物（`templates/Bundle/` 内の `.css` や `.js` など）を直接編集しないでください。
*   **新しいテンプレートの作成**: 新しいページや機能を追加する場合は、`templates/` 内の適切なサブディレクトリに配置してください。
*   **エラー対応**: 同期エラーが発生した場合、`sync.js` の設定や `.env` の認証情報を確認する必要があるかもしれません（ただし、認証情報は直接出力しないでください）。
*   **ドメイン知識の活用**: `.knowledge/` ディレクトリには、プロジェクト固有のルールやドメイン知識が含まれています。実装方針に迷った際や、仕様を確認したい場合は、必ずこのディレクトリ内のドキュメントを確認してください。

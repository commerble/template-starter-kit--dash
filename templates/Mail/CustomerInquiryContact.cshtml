@{
  Message.Subject = "お問い合わせ受付完了のお知らせ";
}お問い合わせいただきありがとうございます。

以下の内容でお問い合わせを承りました。

■お名前：@(ViewBag.Parameters["LastName"]) @(ViewBag.Parameters["FirstName"])
■お名前フリガナ：@(ViewBag.Parameters["LastKana"]) @(ViewBag.Parameters["FirstKana"])
■メールアドレス：@(ViewBag.Parameters["Email"])
■注文番号： @(ViewBag.Parameters["OrderId"])
■お問い合わせ内容：
@(ViewBag.Parameters["Text"])
